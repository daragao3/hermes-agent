---
title: "Impeccable — 前端设计指导，由上游维护（impeccable）"
sidebar_label: "Impeccable"
description: "前端设计指导，由上游维护（impeccable）"
---

{/* This page is auto-generated from the skill's SKILL.md by website/scripts/generate-skill-docs.py. Edit the source SKILL.md, not this page. */}

# Impeccable

前端设计指导，由上游维护（impeccable）。

## Skill 元数据 {#skill-metadata}

| | |
|---|---|
| 来源 | 可选 —— 通过 `hermes skills install official/creative/impeccable` 安装 |
| 路径 | `optional-skills/creative/impeccable` |
| 版本 | `4.1.2` |
| 作者 | Paul Bakaus (pbakaus) |
| 许可证 | Apache-2.0 |
| 平台 | linux, macos, windows |
| 标签 | `design`, `frontend`, `ui`, `ux`, `web-design`, `anti-slop` |
| 相关 skill | [`claude-design`](/user-guide/skills/bundled/creative/creative-claude-design), [`popular-web-designs`](/user-guide/skills/bundled/creative/creative-popular-web-designs) |

## 参考：完整 SKILL.md {#reference-full-skillmd}

:::info
以下是 Hermes 在触发该 skill 时加载的完整 skill 定义。这是 agent 在 skill 激活时所看到的指令内容。
:::

# Impeccable（由上游维护） {#impeccable-upstream-maintained}

> **目录占位条目。** 本条目由上游
> [pbakaus/impeccable](https://github.com/pbakaus/impeccable) 维护：该项目
> 在 `.hermes/skills/` 下发布并验证一个 Hermes 原生的 skill 包。
> `hermes skills install impeccable` 会从该仓库实时拉取当前的 skill 包
> （和任何 hub 安装一样经过隔离与扫描）—— 本目录只保存目录元数据，
> 因此内置副本永远不会过时。

Impeccable 是一套面向 AI 编码 agent 的设计语言：一个 skill 提供 23 个
子命令（`/impeccable init`、`craft`、`shape`、`critique`、`audit`、
`polish`、`bolder`、`quieter`、`distill`、`harden`、`onboard`、`animate`、
`colorize`、`typeset`、`layout`、`delight`、`overdrive`、`clarify`、`adapt`、
`optimize`、`extract`、`document`、`live`），明确的反模式指导
（被滥用的字体、紫色渐变、嵌套卡片、弹跳缓动），以及一个包含
61 条规则的确定性检测 CLI（`npx impeccable detect`），无需任何
LLM 或 API 密钥。

安装后，从这里开始：

```
/impeccable init
```

完整文档：https://impeccable.style
